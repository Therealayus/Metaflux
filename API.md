# API

Versioned under `/api/v1`: auth, organizations, workspaces, connections, assets,
capabilities, permissions, workflows, executions, webhooks, events, health, usage,
billing. Zod validation, typed DTOs, structured errors `{code,message,requestId}`,
cursor pagination, idempotency keys.
